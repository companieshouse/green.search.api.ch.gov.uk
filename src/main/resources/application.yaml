spring:
  application:
    name: green.search.api.ch.gov.uk
management:
  endpoints:
    enabled-by-default: false
    web:
      base-path: /
      path-mapping:
        health: /green-search-api/healthcheck
  endpoint:
    health:
      enabled: true
      show-details: never
